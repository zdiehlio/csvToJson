{
  "name": "csvparser",
  "version": "1.0.0",
  "description": "stream csv and convert into json",
  "main": "csvToJson.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "convert": "node ./csvToJson.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "csvtojson": "^2.0.10"
  }
}
